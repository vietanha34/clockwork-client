# Implementation Plan: Clickable Issue Keys

**Track ID:** clickable-issue-keys_20260225
**Spec:** [spec.md](./spec.md)
**Created:** 2026-02-25
**Status:** [x] Implemented

## Overview

Add a `JIRA_BASE_URL` constant and a shared helper function to open issue URLs in the browser, then update ActiveTimer and WorklogList components to make issue keys clickable.

## Phase 1: Foundation

Add the constant and helper utility for opening issue URLs.

### Tasks

- [x] Task 1.1: Add `JIRA_BASE_URL` constant to `apps/tauri/src/lib/constants.ts`
- [x] Task 1.2: Create `openIssueInBrowser(issueKey: string)` helper in `apps/tauri/src/lib/utils.ts` — constructs URL `${JIRA_BASE_URL}/browse/${issueKey}` and calls `open()` from `@tauri-apps/plugin-shell`

### Verification

- [ ] `JIRA_BASE_URL` is defined and exported
- [ ] `openIssueInBrowser` function compiles without errors

## Phase 2: UI Integration

Make issue keys clickable in both components.

### Tasks

- [x] Task 2.1: Update `apps/tauri/src/components/ActiveTimer.tsx` — wrap issue key `<span>` with a `<button>` that calls `openIssueInBrowser()`, add `cursor-pointer hover:underline` styles
- [x] Task 2.2: Update `apps/tauri/src/components/WorklogList.tsx` — wrap issue key `<span>` with a `<button>` that calls `openIssueInBrowser()`, add `cursor-pointer hover:underline` styles

### Verification

- [ ] Clicking issue key in ActiveTimer opens correct Jira URL in browser
- [ ] Clicking issue key in WorklogList opens correct Jira URL in browser
- [ ] Hover state shows underline and pointer cursor
- [ ] No layout or spacing regressions

## Final Verification

- [ ] All acceptance criteria met
- [ ] No regressions in existing features
- [ ] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
