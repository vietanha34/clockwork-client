# Implementation Plan: Vercel Deployment

**Track ID:** vercel-deployment_20260210

## Phase 1: Inngest Vercel Adapter

**Goal:** Add a Vercel-compatible API route handler for Inngest alongside the existing Express entry point.

### ~~Task 1.1~~ — [x] Add `inngest/vercel` serve handler

Create `apps/inngest/api/inngest.ts` using the `serve` adapter from `inngest/vercel`. This file exports a default handler that Vercel deploys as a serverless function at `/api/inngest`.

**Files:** `apps/inngest/api/inngest.ts`

### ~~Task 1.2~~ — [x] Add `vercel.json` for Inngest app

Create `apps/inngest/vercel.json` with appropriate config: `@vercel/node` runtime, route rewrites, and build/install commands for the monorepo.

**Files:** `apps/inngest/vercel.json`

### ~~Task 1.3~~ — [x] Verify type-check and lint

Run `pnpm turbo type-check lint` to ensure the new Vercel handler compiles and passes linting.

**Verification:** `pnpm turbo type-check lint` — 6/6 successful, 0 errors, 0 warnings. ✓

---

## Phase 2: API App Vercel Config Verification

**Goal:** Verify and update the existing `apps/api/vercel.json` for correct monorepo deployment.

### ~~Task 2.1~~ — [x] Review and update `apps/api/vercel.json`

Verify the existing `vercel.json` has correct `buildCommand`, `installCommand`, and route configuration for the monorepo root. Ensure the `@vercel/node` runtime is properly configured for all API routes.

**Files:** `apps/api/vercel.json` — no changes needed, existing config is correct.

### ~~Task 2.2~~ — [x] Verify type-check and lint

Run `pnpm turbo type-check lint` to ensure everything still passes.

**Verification:** `pnpm turbo type-check lint` — 6/6 successful, 0 errors, 0 warnings. ✓

---

## Phase 3: Deployment Documentation

**Goal:** Create deployment documentation with environment variable checklists and step-by-step instructions.

### Task 3.1 — Create deployment guide

Create `docs/deployment.md` with:
- Prerequisites (Vercel account, Inngest account, Upstash Redis)
- Environment variable checklist for each app
- Step-by-step Vercel CLI deployment commands
- Post-deployment verification steps (health check, Inngest dashboard)

**Files:** `docs/deployment.md`

### Task 3.2 — Final verification and track completion

Run full type-check and lint. Verify all files are committed.

**Verification:** `pnpm turbo type-check lint` — 0 errors, 0 warnings.

---

_Generated by Conductor._
