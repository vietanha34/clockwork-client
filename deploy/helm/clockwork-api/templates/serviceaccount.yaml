{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "clockwork-api.serviceAccountName" . }}
  labels:
    {{- include "clockwork-api.labels" . | nindent 4 }}
{{- end }}
