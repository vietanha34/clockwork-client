{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev --listen 3000",
    "build": "tsc",
    "lint": "biome check .",
    "type-check": "tsc --noEmit",
    "inngest:dev": "inngest-cli dev"
  },
  "dependencies": {
    "inngest": "^3.52.0",
    "redis": "^5.10.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@clockwork-menubar/typescript-config": "workspace:*",
    "@types/node": "^20.0.0",
    "@vercel/node": "^5.0.0",
    "inngest-cli": "^1.16.3"
  }
}
