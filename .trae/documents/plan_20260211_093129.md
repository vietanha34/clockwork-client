I will implement the changes to use `displayName` as the primary mapping key for users across the system.

### 1. Update Jira Client & Resolver

**Files**: `apps/api/src/lib/atlassian-client.ts`, `apps/api/src/lib/jira-user-resolver.ts`

* **Add Bulk User Fetch**: Implement `getJiraUsersBulk(accountIds)` in `atlassian-client.ts` using the `/rest/api/3/user/bulk` endpoint.

* **Update Resolver**: Modify `resolveTimerAuthors` in `jira-user-resolver.ts` to use the bulk API to fetch user details (specifically `displayName`) for all timer authors efficiently.

### 2. Update Inngest Sync Logic

**File**: `apps/api/src/inngest/sync-active-timers.ts`

* **Group by Display Name**: Instead of grouping timers by `accountId`, group them by `author.displayName`.

* **Cache with Display Name**: Store the grouped timers in Redis using keys based on `displayName` (e.g., `clockwork:timers:<displayName>`).

### 3. Update Resolve Endpoint

**File**: `apps/api/api/users/resolve.ts`

* **Return Display Name**: Ensure the endpoint returns `displayName`.

* **Support Query**: Update the endpoint to support searching/resolving by `displayName` (via `query` parameter) in addition to email, using a generic `searchJiraUser` function.

### 4. Update Active Timers Endpoint

**File**: `apps/api/api/timers/active.ts`

* **Query by Display Name**: Update the endpoint to accept a `displayName` query parameter (instead of or in addition to `accountId`).

* **Fetch from Redis**: Use the `displayName` to look up the correct timer list from Redis.

### 5. Update Client (Optional but recommended)

**File**: `apps/tauri/src/lib/api-client.ts`

* **Reflect Changes**: Update `fetchActiveTimers` to use the `displayName` parameter.

