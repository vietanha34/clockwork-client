I will update both the API and Tauri app to use `accountId` instead of `userEmail` for fetching worklogs, as requested.

### Backend Changes (`apps/api`)

1. **`apps/api/src/lib/types.ts`**:

   * Update `WorklogsResponse` interface: replace `userEmail: string` with `accountId: string`.

2. **`apps/api/src/lib/clockwork-client.ts`**:

   * Update `getWorklogs` function signature to accept `accountId: string` instead of `userEmail`.

   * Update the query parameter sent to Clockwork API from `userEmail` to `accountId` (assuming Clockwork API supports this, which is standard for Jira integrations).

3. **`apps/api/api/worklogs.ts`**:

   * Update request query parsing to extract `accountId` instead of `userEmail`.

   * Validate `accountId`.

   * Pass `accountId` to `getWorklogs`.

   * Update the response object to include `accountId` instead of `userEmail`.

### Frontend Changes (`apps/tauri`)

1. **`apps/tauri/src/lib/types.ts`**:

   * Update `WorklogsResponse` interface: replace `userEmail: string` with `accountId: string`.

2. **`apps/tauri/src/lib/api-client.ts`**:

   * Update `fetchWorklogs` function signature to accept `accountId: string` instead of `userEmail`.

   * Update the query parameter sent to the backend API from `userEmail` to `accountId`.

3. **`apps/tauri/src/hooks/useWorklogs.ts`**:

   * Update `useWorklogs` hook to pass `jiraToken` (which now holds the Account ID) as the `accountId` argument to `fetchWorklogs`.

   * Update the query key to use `accountId` (alias of `jiraToken`) for clarity.

4. **`apps/tauri/src/views/SettingsView.tsx`**:

   * (Optional verification) Ensure the input field label and placeholder reflect that the user should enter their Jira Account ID (even if the internal variable is named `jiraToken` as per previous instructions).

